{
  "name": "radshare.io",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "cd api && npm start",
    "heroku-postbuild": "cd client && npm install && npm build"
  },
  "dependencies": {
    "ts-node": "~8.3.0"
  }
}
