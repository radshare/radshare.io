{
  "name": "radshare.io",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "cd api && npm start",
    "preinstall": "npm install -g angular-cli",
    "postinstall": "cd client && ng build"
  }
}
