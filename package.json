{
  "name": "radshare.io",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "cd api/ && npm install && npm run build && npm start",
    "heroku-postbuild": "cd client && npm install"
  },
  "engines": {
    "npm": "6.14.8",
    "node": "12.14.1"
  }
}
