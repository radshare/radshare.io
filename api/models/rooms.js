const mongoose = require('mongoose');

const roomSchema = new mongoose.Schema({
    relic: {
        type: String,
        unique: true,
        required: true
    },
    name: {
        type: String,
        required: true
    },
    hash: String,
    salt: String
});
